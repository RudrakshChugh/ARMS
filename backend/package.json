{
  "name": "ucs503-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "cross-env NODE_ENV=test STORAGE_PROVIDER=local DATABASE_URL=postgresql://postgres:postgres@localhost:5432/semester_portal_test vitest run",
    "test:watch": "cross-env NODE_ENV=test STORAGE_PROVIDER=local DATABASE_URL=postgresql://postgres:postgres@localhost:5432/semester_portal_test vitest",
    "test:coverage": "cross-env NODE_ENV=test STORAGE_PROVIDER=local DATABASE_URL=postgresql://postgres:postgres@localhost:5432/semester_portal_test vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1106.0",
    "@aws-sdk/s3-request-presigner": "^3.1106.0",
    "@supabase/supabase-js": "^2.112.3",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "cross-env": "^10.1.0",
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
